{
  "name": "@image-upload/api",
  "version": "0.1.0",
  "description": "API Client（Orvalによる型・Client生成）",
  "main": "src/index.ts",
  "scripts": {
    "generate": "orval",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "type-check": "tsc --noEmit",
    "doc": "typedoc --entryPoints src/index.ts --out docs --skipErrorChecking"
  },
  "dependencies": {
    "@tanstack/react-query": "catalog:frontend",
    "axios": "^1.7.2",
    "zod": "catalog:default"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:default",
    "@types/node": "catalog:default",
    "orval": "^7.4.0",
    "typedoc": "catalog:default",
    "typescript": "catalog:default"
  }
}

